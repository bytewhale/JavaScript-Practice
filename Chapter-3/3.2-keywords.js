'use strict'

/**
 * @description: 关键字与保留字
 */

console.log(`
    ECMA-262 描述了一组保留的【关键字】，这些关键字有特殊用途，比如表示控制语句的开始和结束，
    或者执行特地的操作。按照规定，保留的关键字不能用作标示符或属性名。
    ECMA-262 第 6 版规定的所有关键字如下：
    
    break     do          in             typeof
    case      else        instanceof     var
    catch     export      new            void
    class     extends     return         while
    const     finally     super          with
    debugger  function    this
    default   if          throw
    delete    import      try
`)
